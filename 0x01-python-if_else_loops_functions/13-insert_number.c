#include "lists.h"
/**
 * insert_node - inserting a node at a certian point
 * @head: this is head of the list
 * @number: this is the number that you feed in
 * return: the linked list
 */
listint_t *insert_node(listint_t **head, int number)
{
	(void) head;
	(void) number;
	return (NULL);
}
